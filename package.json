{
  "name": "the-jobsync-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "node backend/index.js",
    "start": "start-servers.bat"
  },
  "dependencies": {
    "mongoose": "^9.9.4",
    "nodemailer": "^9.0.3"
  }
}
